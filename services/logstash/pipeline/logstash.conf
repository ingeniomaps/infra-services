# Pipeline mínimo. Ajustar input/filter/output a tu proyecto.
input {
  stdin { }
}

filter {
  # ej: grok, json, etc.
}

output {
  stdout { codec => rubydebug }
  # ej: elasticsearch { hosts => ["elasticsearch:9200"] }
}
