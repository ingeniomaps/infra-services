# Logstash - Pipeline de procesamiento de logs.
# Pipeline en services/logstash/pipeline; config en logstash/config.
# El pipeline por defecto es un placeholder (stdin/stdout). Ajustar para producción.
# Red: docker network create ${NETWORK_NAME}
# Uso: make infra-logstash

name: ${PROJECT_PREFIX:-logstash}${PROJECT_PREFIX:+-logstash}

services:
  logstash:
    image: docker.elastic.co/logstash/logstash:9.3.1
    container_name: ${PROJECT_PREFIX:-logstash}${PROJECT_PREFIX:+-logstash}
    environment:
      LS_JAVA_OPTS: ${LS_JAVA_OPTS:--Xms512m -Xmx512m}
    volumes:
      - ${INFRA_SERVICES_PATH}/logstash/pipeline:/usr/share/logstash/pipeline:ro
      - ${INFRA_SERVICES_PATH}/logstash/config:/usr/share/logstash/config:ro
    networks:
      - default
    ports:
      - '${LOGSTASH_PORT:-5044}:5044'
    healthcheck:
      test: ['CMD-SHELL', 'curl -f http://localhost:9600/_node/stats || exit 1']
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 2G
          cpus: '1'
        reservations:
          memory: 512M
          cpus: '0.25'

networks:
  default:
    name: ${NETWORK_NAME}
    external: true
